/// @description Insert description here
// You can write your code in this editor

if(currentHP <= 0)
{
	instance_destroy();
}

//play animation once then pause
if(image_index = image_number - 1)
{
	image_speed = 0;
}